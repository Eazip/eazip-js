---
title: StartZipOptions
description: Options accepted by the framework-agnostic `startZip()` and `createZip()` APIs.
---

> **StartZipOptions** = [`LocalZipOptions`](LocalZipOptions) \| [`CloudZipOptions`](CloudZipOptions)

Defined in: [packages/core/src/shared/types.ts:278](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/shared/types.ts#L278)

Options accepted by the framework-agnostic `startZip()` and `createZip()` APIs.
