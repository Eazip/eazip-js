---
title: staggerDownloads
description: Runs `download(i)` for each index with a stagger between clicks.
---

> **staggerDownloads**(`count`, `download`): `void`

Defined in: [packages/core/src/shared/download.ts:33](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/shared/download.ts#L33)

Runs `download(i)` for each index with a stagger between clicks.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `count` | `number` |
| `download` | (`index`) => `void` |

## Returns

`void`
