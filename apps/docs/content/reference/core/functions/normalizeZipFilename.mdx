---
title: normalizeZipFilename
description: Sanitizes a requested archive filename and ensures it ends in `.zip`.
---

> **normalizeZipFilename**(`filename`): `string`

Defined in: [packages/core/src/local/filenames.ts:4](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/local/filenames.ts#L4)

Sanitizes a requested archive filename and ensures it ends in `.zip`.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `filename` | `string` \| `undefined` |

## Returns

`string`
