---
title: linkAbort
description: Abort `target` when `source` aborts (immediately if already aborted).
---

> **linkAbort**(`source`, `target`): `void`

Defined in: [packages/core/src/shared/abort.ts:15](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/shared/abort.ts#L15)

Abort `target` when `source` aborts (immediately if already aborted).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `source` | `AbortSignal` |
| `target` | `AbortController` |

## Returns

`void`
