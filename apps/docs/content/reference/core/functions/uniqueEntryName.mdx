---
title: uniqueEntryName
description: Reserves a ZIP entry path, adding a numeric suffix when it is already used.
---

> **uniqueEntryName**(`filename`, `used`): `string`

Defined in: [packages/core/src/local/filenames.ts:15](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/local/filenames.ts#L15)

Reserves a ZIP entry path, adding a numeric suffix when it is already used.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `filename` | `string` |
| `used` | `Set`\<`string`\> |

## Returns

`string`
