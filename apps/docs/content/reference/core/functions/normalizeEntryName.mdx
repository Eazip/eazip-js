---
title: normalizeEntryName
description: Sanitizes a ZIP entry path and falls back when the result would be empty.
---

> **normalizeEntryName**(`filename`, `fallback`): `string`

Defined in: [packages/core/src/local/filenames.ts:10](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/local/filenames.ts#L10)

Sanitizes a ZIP entry path and falls back when the result would be empty.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `filename` | `string` \| `undefined` |
| `fallback` | `string` |

## Returns

`string`
