---
title: createZip
description: Starts a local or Cloud ZIP job and waits for its result. This is the one-shot alternative to `startZip()`. The returned promise rejects for validation, cancellation, and fatal job errors.
---

## Call Signature

> **createZip**(`options`): `Promise`\<[`LocalZipResult`](../type-aliases/LocalZipResult)\>

Defined in: [packages/core/src/index.ts:35](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/index.ts#L35)

Starts a local or Cloud ZIP job and waits for its result.

This is the one-shot alternative to `startZip()`. The returned promise
rejects for validation, cancellation, and fatal job errors.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`LocalZipOptions`](../type-aliases/LocalZipOptions) |

### Returns

`Promise`\<[`LocalZipResult`](../type-aliases/LocalZipResult)\>

## Call Signature

> **createZip**(`options`): `Promise`\<[`CloudZipResult`](../type-aliases/CloudZipResult)\>

Defined in: [packages/core/src/index.ts:36](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/index.ts#L36)

Starts a local or Cloud ZIP job and waits for its result.

This is the one-shot alternative to `startZip()`. The returned promise
rejects for validation, cancellation, and fatal job errors.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`CloudZipOptions`](../type-aliases/CloudZipOptions) |

### Returns

`Promise`\<[`CloudZipResult`](../type-aliases/CloudZipResult)\>

## Call Signature

> **createZip**(`options`): `Promise`\<[`ZipResult`](../type-aliases/ZipResult)\>

Defined in: [packages/core/src/index.ts:37](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/index.ts#L37)

Starts a local or Cloud ZIP job and waits for its result.

This is the one-shot alternative to `startZip()`. The returned promise
rejects for validation, cancellation, and fatal job errors.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`StartZipOptions`](../type-aliases/StartZipOptions) |

### Returns

`Promise`\<[`ZipResult`](../type-aliases/ZipResult)\>
