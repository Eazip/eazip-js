---
title: toSourceFiles
description: Normalizes any accepted `files` input into EazipSourceFile[]. Throws `EazipValidationError` synchronously for empty or invalid input.
---

> **toSourceFiles**(`input`): [`EazipSourceFile`](../type-aliases/EazipSourceFile)[]

Defined in: [packages/core/src/shared/input.ts:8](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/shared/input.ts#L8)

Normalizes any accepted `files` input into EazipSourceFile[].
Throws `EazipValidationError` synchronously for empty or invalid input.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `input` | [`ZipInput`](../type-aliases/ZipInput) |

## Returns

[`EazipSourceFile`](../type-aliases/EazipSourceFile)[]
