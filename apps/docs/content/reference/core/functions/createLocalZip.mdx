---
title: createLocalZip
description: Creates a ZIP in the browser and resolves when all ZIP parts are ready.
---

> **createLocalZip**(`options`): `Promise`\<[`LocalZipResult`](../type-aliases/LocalZipResult)\>

Defined in: [packages/core/src/local/engine.ts:64](https://github.com/Eazip/eazip-js/blob/main/packages/core/src/local/engine.ts#L64)

Creates a ZIP in the browser and resolves when all ZIP parts are ready.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`LocalZipOptions`](../type-aliases/LocalZipOptions) |

## Returns

`Promise`\<[`LocalZipResult`](../type-aliases/LocalZipResult)\>
