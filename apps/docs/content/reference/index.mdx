---
title: Reference
description: Generated public API reference for @eazip/core and @eazip/react.
---

Choose the package whose exported functions, types, and classes you need to
inspect. These pages are generated from the TypeScript source, so signatures
stay aligned with the packages.

## `@eazip/core`

The framework-agnostic browser engine and the shared Local / Cloud job model.

[Browse `@eazip/core` symbols →](/docs/reference/core)

For task-oriented explanation, start with [Core concepts](/docs/concepts).

## `@eazip/react`

The React hook, provider, tray, localization, persistence, and headless APIs.

[Browse `@eazip/react` symbols →](/docs/reference/react)

For integration patterns and examples, start with
[React integration](/docs/react).

Cloud HTTP endpoints are intentionally not a third package in this Reference.
Start with [When to use Eazip Cloud](/docs/cloud); the HTTP API contract belongs
to the separate Cloud documentation area.
