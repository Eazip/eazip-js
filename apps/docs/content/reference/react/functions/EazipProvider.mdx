---
title: EazipProvider
description: Optional provider. `useEazip()` and `<EazipTray />` work without it via a shared default store; wrap your app in `EazipProvider` to set shared config (publicKey, strategy, defaults) or to isolate state (tests, multiple roots).
---

> **EazipProvider**(`__namedParameters`): `ReactNode`

Defined in: [context.tsx:24](https://github.com/Eazip/eazip-js/blob/main/packages/react/src/context.tsx#L24)

Optional provider. `useEazip()` and `<EazipTray />` work without it via a
shared default store; wrap your app in `EazipProvider` to set shared config
(publicKey, strategy, defaults) or to isolate state (tests, multiple roots).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`EazipProviderProps`](../type-aliases/EazipProviderProps) |

## Returns

`ReactNode`
