---
title: useEazip
description: Returns ZIP commands and the latest task state from the nearest Eazip store. It uses `EazipProvider` when present and otherwise falls back to the shared browser store.
---

> **useEazip**(): [`UseEazipResult`](../type-aliases/UseEazipResult)

Defined in: [use-eazip.ts:13](https://github.com/Eazip/eazip-js/blob/main/packages/react/src/use-eazip.ts#L13)

Returns ZIP commands and the latest task state from the nearest Eazip store.

It uses `EazipProvider` when present and otherwise falls back to the shared
browser store.

## Returns

[`UseEazipResult`](../type-aliases/UseEazipResult)
